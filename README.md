# mini-container-book

本文は [book/README.md](book/README.md) から読めます。

本文に登場するサンプルコードの完全版は [examples/README.md](examples/README.md) にあります。

## Build

Typst source can be generated with:

```bash
make typst
```

PDF output requires the Typst CLI and can be generated with:

```bash
make pdf
```

Outputs are written to `dist/mini-container-book.typ` and `dist/mini-container-book.pdf`.
